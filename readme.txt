Testing AbortController signal

a) running the server api

1. cd server
2. npm start (after npm install, of course)

b) running the frontend in another terminal

3. cd ../frontend
4. npm run dev (after npm install)
5. load webpage http://localhost:5173/

